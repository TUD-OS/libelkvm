set( proxy_SRCS main.cc )
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -msse4")
include_directories("${CMAKE_INSTALL_PREFIX}/include")
add_executable( proxy ${proxy_SRCS})
target_link_libraries( proxy elkvm )
